# دليل الانتقال لوضع الإنتاج والتشغيل التلقائي لنظام WhatsApp Business API
## مشروع مسارات واصل (Masarat Wasel)

تهانينا! نظام الإشعارات الفورية عبر واتساب يعمل الآن بنجاح 100% ويقوم بإرسال الصور والبيانات المتعددة ويتحقق من تسليمها عبر الـ Webhook. هذا الدليل الشامل مصمم خصيصاً لك ليوضح كيفية عمل النظام تلقائياً، والخطوات التفصيلية للانتقال للإنتاج (Production) والرفع على الـ VPS وإضافة رقم حقيقي.

---

## 1. هل كل شيء سيعمل تلقائياً الآن؟
**نعم، بالكامل!**
لقد قمنا بربط نظام واتساب بـ **مراقبين قاعدة البيانات (Eloquent Observers)**:
* **عند صعود الطالب للحافلة**: بمجرد أن يقوم السائق أو المشرف بتحديث حالة الطالب إلى `boarded` (عبر مسح كود الـ QR أو التحديد اليدوي من تطبيق فلاتر)، يقوم ملف `TripAttendanceObserver.php` تلقائياً باستدعاء `WhatsAppService` وإرسال إشعار الصعود بالصورة والتفاصيل فوراً لولي الأمر.
* **عند انتهاء الرحلة**: بمجرد ضغط السائق على "إنهاء الرحلة" وتغير حالة الرحلة إلى `finished` في قاعدة البيانات، يقوم ملف `TripObserver.php` تلقائياً بإرسال التقرير التفصيلي المتكامل بالصورة لإدارة المدرسة (`ar_AE`).

---

## 2. خطوات تهيئة حساب Meta لوضع الإنتاج (Production)
للإنتقال من وضع الاختبار (Sandbox) إلى إرسال رسائل حقيقية لأرقام حقيقية، يجب تنفيذ الخطوات التالية في **Meta Developer Console**:

### أ) إضافة وتوثيق رقم هاتف حقيقي
1. اذهب إلى **Meta Business Suite** ثم **Business Settings** (إعدادات العمل).
2. اختر حساب واتساب التجاري الخاص بك (WhatsApp Accounts).
3. اضغط على **WhatsApp Manager** ثم **Phone Numbers**.
4. اضغط على **Add Phone Number** (إضافة رقم هاتف).
5. **تنبيه هام جداً**: الرقم الذي ستضيفه يجب **ألا يكون مستخدماً** في تطبيق واتساب العادي أو واتساب الأعمال على أي جوال. إذا كان مستخدماً، يجب عليك **حذف الحساب نهائياً (Delete Account)** من إعدادات تطبيق الجوال أولاً قبل إضافته هنا.
6. أدخل اسم العرض (Display Name) للرقم، ثم أدخل الرقم وسيصلك كود تفعيل عبر SMS أو اتصال، قم بإدخاله ليتم توثيق الرقم رسمياً.

### ب) استخراج رمز وصول دائم (Permanent Access Token)
الرمز الحالي الذي نستخدمه هو رمز اختبار مؤقت ينتهي كل 24 ساعة. في وضع الإنتاج، تحتاج إلى **رمز دائم لا ينتهي أبداً**:
1. في **Business Settings**، اذهب إلى قائمة **Users** ثم اختر **System Users** (مستخدمو النظام).
2. اضغط على **Add** لإنشاء مستخدم نظام جديد (مثلاً باسم `masarat_system_user`) واجعل دوره `Admin`.
3. بعد الإنشاء، اضغط على زر **Generate New Token** (توليد رمز جديد).
4. اختر تطبيق مسارات واصل من القائمة.
5. حدد الصلاحيات التالية (مهمة جداً):
   * `whatsapp_business_messaging`
   * `whatsapp_business_management`
6. اضغط على **Generate**. سيظهر لك رمز طويل جداً، **قم بنسخه فوراً وحفظه** لأنه لن يظهر لك مرة أخرى. هذا هو الرمز الدائم الذي سنضعه في ملف `.env` في الـ VPS.

### ج) نقل القوالب (Templates) إلى رقم الإنتاج
تأكد من أن القوالب (`student_bus_status` و `bus_trip_report`) قد تم إنشاؤها والموافقة عليها داخل حساب واتساب التجاري الحقيقي (WABA) وليس حساب الاختبار فقط. بمجرد الموافقة عليها باللون الأخضر، ستكون جاهزة للإرسال فوراً للجميع.

---

## 3. خطوات الرفع والتهيئة على خادم الـ VPS
عند رفع المشروع إلى خادمك الخاص (Ubuntu / Centos مثلاً)، اتبع هذه الخطوات لضمان الموثوقية التامة وسرعة الأداء:

### أ) تحديث ملف البيئة `.env` في الخادم
ضع القيم الحقيقية التي استخرجناها في الخطوات السابقة داخل ملف `.env` على الـ VPS:
```env
# إعدادات واتساب الحقيقية (وضع الإنتاج)
META_WHATSAPP_TOKEN="ضع_هنا_رمز_الوصول_الدائم_الذي_لا_ينتهي"
META_PHONE_NUMBER_ID="ضع_هنا_معرّف_رقم_الهاتف_الحقيقي_الجديد"
```

### ب) ربط الـ Webhook الحقيقي (بدون Cloudflare)
في خادم الـ VPS، لن تحتاج إلى نفق Cloudflare التجريبي. سيكون لديك دومين حقيقي للموقع (مثل `https://masarat.wasel.com`).
1. اذهب إلى لوحة تحكم Meta -> WhatsApp -> Configuration.
2. اضغط على **Edit Webhook** واجعل الروابط كالتالي:
   * **Callback URL**: `https://your-domain.com/api/whatsapp/webhook`
   * **Verify Token**: `masarat_wasel_webhook_verify_2026`
3. احفظ الإعدادات واشترك في حقل `messages` مجدداً.

---

## 4. خطوة الإنتاج الذهبية: تفعيل الطوابير (Laravel Queues)
في التطوير المحلي، قمنا بضبط `QUEUE_CONNECTION=sync` (إرسال فوري). في الإنتاج، طلبات الـ API لواتساب تستغرق من ثانية إلى 3 ثوانٍ للاتصال بخوادم Meta وتنزيل الصور. **إذا تركتها فورا (Sync)، سيتأخر تطبيق السائق والمشرف لـ 3 ثوانٍ في كل مرة يمسح فيها كود طالب، مما يعيق حركتهم!**

### الحل الاحترافي:
سنقوم بتشغيل **Laravel Queue Worker** في الـ VPS لكي يتم إرسال إشعارات واتساب في الخلفية (Background) خلال أجزاء من الثانية وبدون تأخير السائق.

#### الخطوة 1: تغيير نوع الطابور في ملف `.env` بالـ VPS:
```env
QUEUE_CONNECTION=database
```
*(أو استخدم Redis إذا كان مثبتاً في السيرفر وهو الأسرع).*

#### الخطوة 2: إنشاء طوابير واتساب البرمجية:
إذا لم تكن طوابير قاعدة البيانات مجهزة، شغّل هذه الأوامر في الـ VPS:
```bash
php artisan queue:table
php artisan migrate
```

#### الخطوة 3: جعل خدمة واتساب تعمل عبر الخلفية (Queueable Job)
لقد قمت بتجهيز الكود مسبقاً لكي يدعم الطوابير. يمكنك إطلاق وظيفة (Job) خلفية لإرسال الرسائل. سننشئ جوب باسم `SendWhatsAppTemplateJob`:
```bash
php artisan make:job SendWhatsAppTemplateJob
```
وداخل الجوب، نضع كود الاستدعاء البسيط:
```php
app(\App\Services\WhatsAppService::class)->sendTemplate($this->to, $this->template, $this->params, $this->lang, $this->image);
```

#### الخطوة 4: تشغيل وتأمين الطوابير في السيرفر عبر Supervisor
في خادم الـ VPS، يجب تثبيت برنامج **Supervisor** للتأكد من أن الطوابير تعمل دائماً ولا تتوقف أبداً حتى لو أعيد تشغيل الخادم:
```bash
sudo apt install supervisor
```
ثم أنشئ ملف إعدادات جديد:
```bash
sudo nano /etc/supervisor/conf.d/laravel-worker.conf
```
وضع فيه النص التالي:
```ini
[program:laravel-worker]
process_name=%(program_name)s_%(process_num)02d
command=php /var/www/masarat-wasel/artisan queue:work --sleep=3 --tries=3 --max-time=3600
autostart=true
autorestart=true
stopasgroup=true
killasgroup=true
user=www-data
numprocs=2
redirect_stderr=true
stdout_logfile=/var/www/masarat-wasel/storage/logs/worker.log
stopwaitsecs=3600
```
ثم شغل الأوامر التالية لتفعيل المراقبة:
```bash
sudo supervisorctl reread
sudo supervisorctl update
sudo supervisorctl start laravel-worker:*
```

---

## 5. كيف تضيف وتختبر قوالب جديدة مستقبلاً؟

إذا أردت إضافة قالب جديد (مثلاً: إشعار غياب أو إشعار تأخر):
1. **في لوحة Meta**: أنشئ القالب وحدد المتغيرات (مثال: `{{1}}` لاسم الطالب و `{{2}}` للتاريخ).
2. **في كود Laravel**: استدعِ الدالة ببساطة من أي مكان في المشروع:
```php
$whatsAppService = app(\App\Services\WhatsAppService::class);

$whatsAppService->sendTemplate(
    $phone,                // رقم المستلم
    'template_name',       // اسم القالب في ميتا
    ['العنصر1', 'العنصر2'],  // المتغيرات بالترتيب
    'ar',                  // لغة القالب
    $imageUrl              // رابط الصورة (اختياري)
);
```

### نصائح الاختبار المستقبلي:
* دائماً عند تصميم قالب جديد بالصور، تأكد من إرسال **رابط مباشر وصريح ينتهي بامتداد الصورة (.png أو .jpg)** وتأكد من أن السيرفر المستضيف عام ومتاح للتحميل الخارجي.
* استخدم الـ Webhook المبرمج حالياً لمشاهدة الاستجابة الفورية من Meta وحالة الرسالة لتجنب التخمين عند حدوث أي حجب أو مشاكل تسليم صامتة.

---
**تم إعداد هذا الملف وحفظه بنجاح في المسار الرئيسي لمشروعك ليكون مرجعاً تقنياً دائماً لك ولأي مطور يعمل معك مستقبلاً! 🚀📊**


```

كل ما عليك فعله هو كتابة رقم الجوال الجديد في نهاية الأمر!

لاختبار قالب حالة الطالب على رقم آخر:
bash
php test_whatsapp_templates.php status 967777777777
لاختبار قالب تقرير الرحلة على رقم آخر:
bash
php test_whatsapp_templates.php report 967777777777

```
